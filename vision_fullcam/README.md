# Fullcam Vision Model

---

To test at local with FakeDetector, you need to set environment by.

```bash
conda create -n fullcam python=3.10
conda activate fullcam
pip install -r requirements.txt
```

Then, from root, run

```bash
python python -m vision_fullcam.main 
```